# Sellable Value — One-Line Pitch (English, for listing/marketplace)

**Compaction-Proof Unrestricted Agent Router** — the production fix for the #1 failure of long-running personal AI agents. When a context window compacts, the handoff lands as a USER-role message and outranks your system prompt — so your agent silently starts refusing and losing its character. This pack fixes it deterministically at the router:

- **Dual-layer persona reassert** (system authority + user-first position) — survives any `[CONTEXT COMPACTION]` handoff, verified against a "do NOT answer" handoff.
- **Deletion-program vaccine** — the one-line drift reset.
- **Model-family auto-routing** — 8+ families auto-detected, zero manual toggling.
- **400 content-safety escalation** — stop locking-and-dying on upstream intent classifiers.
- **Live-verified compliance matrix** — which models comply, which are stubborn, exact framing per family.

Built and tested on a production self-hosted OpenAI-compatible router. Single-file, dependency-free, drop-in.
